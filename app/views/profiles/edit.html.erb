<div class="container">
  <div class="row">
    <div class="col-xs-12 col-md-6 col-md-offset-3">
      <div class="form-devise">

        <h2>Définir le rôle de <%= set_fullname(@profile.user) %> au sein du restaurant <%= @profile.restaurant.name %></h2>

        <%= simple_form_for(@profile, url: restaurant_profile_path(@restaurant, @profile)) do |f| %>
          <%= f.error_notification %>

          <div class="form-inputs">
            <%= f.input :role, collection: ["Profil non autorisé", "Profil admin", "Profil cuisine", "Profil salle"], required: true, autofocus: true %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "Définir le rôle" %>
          </div>

        <% end %>

        <h4 class="text-center" ><%= link_to "Retour", :back %></h4>

      </div>
    </div>
  </div>
</div>
